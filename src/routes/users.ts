// import express from "express";

// import usersController from "../controllers/users.controller";
// import { asyncHandler } from "../middleware/asyncHandler";

// const router = express.Router();

// // 로그인
// router.get("/", asyncHandler(usersController.create));

// // accessToken,
// router.get("/:id", asyncHandler(usersController.read));

// //  refreshToken,
// router.post("/", asyncHandler(usersController.read));

// // loginSuccess,
// router.put("/:id", asyncHandler(usersController.read));

// // 로그아웃
// router.delete("/:id", asyncHandler(usersController.create));

// export default router;
